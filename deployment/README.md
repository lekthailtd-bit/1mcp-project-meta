# Deployment Notes

Non-secret operational notes for staging, release layout, production safeguards, and rollback.

This directory does **not** authorize deployment. Production changes still require explicit user agreement.

Never commit credentials, environment files, tokens, browser profiles, auth state, or secret-bearing logs.
